package com.exory550.exoryfilemanager.dialogs

import android.content.Context

class LicensesDialog(private val context: Context) {

    companion object {
        fun show(fragmentManager: androidx.fragment.app.FragmentManager) {
            // Show licenses
        }
    }
}
